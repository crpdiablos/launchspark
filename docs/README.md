# LaunchSpark v5.0.1 Phoenix — Frontend + Infra
This package includes the React frontend, infra/docker-compose, runner scripts, and docs.

## Usage
1. Place this alongside a `backend/` folder (from LaunchSpark_backend_v5.0.1.zip).
2. Run:
```bash
cd infra
docker compose up --build
```
3. Visit http://localhost:8080
